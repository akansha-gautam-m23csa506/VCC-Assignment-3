version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    restart: always
    container_name: react_frontend
    networks:
      - shared_network

networks:
  shared_network:
    external: true
